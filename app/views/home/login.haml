%h1= t('home.login')
=link_to t("back"), '/'
=render 'partials/notify'

#login_form
  =form_with do |form|
    =form.label :name
    =form.text_field :name
    =form.label :password
    =form.text_field :password
    =form.submit t('home.login')

